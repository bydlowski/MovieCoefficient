$("#result").show();
$("#result").html('<%= input_check(@user_rating, @film_imdb_rating, @film_rotten_rating, @film_tmdb_rating, @film_metacritic_rating).html_safe %>');
// Hide warning box
$(".close-warning").on('click', function() {
  $('#result').hide();
});
$(".userRatings").html('<%= current_coefficient(@imdb_now , @rotten_now, @metacritic_now, @tmdb_now, @amount_now).html_safe %>');
$('#countries').val('');
$('#textSubmit').val('');
$('.suggestion-info').text('');